"""Local ML: GPU-trained audio embedding model for music similarity.

This subpackage is the "train our own model" track, distinct from the offline
DSP engine. It requires the optional `ml` extra (torch, torchaudio).
"""
