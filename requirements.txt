# Core recommender (offline, zero-credential)
numpy>=1.23
pandas>=1.5
scikit-learn>=1.2

# Visualization / personal stats charts
matplotlib>=3.6

# Live integrations (Spotify OAuth + Last.fm). Optional until you add API keys.
requests>=2.28
python-dotenv>=1.0

# Acoustic (DSP) engine: measure audio features from real previews.
librosa>=0.10
soundfile>=0.12

# Tests
pytest>=7.0
